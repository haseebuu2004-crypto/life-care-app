
## Dependency Management
**IMPORTANT:** This project enforces exact dependency versions to prevent drift. Always use `npm ci` for fresh installs instead of `npm install`. Do NOT use `npm install` in CI/CD pipelines or production builds.
